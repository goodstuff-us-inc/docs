# Architecture

## Client-side
All client-side code is written in React Native.

## Server-side
This is a serverless application; all server-side logic exists within a collection of AWS services, all managed via the Amplify framework.
